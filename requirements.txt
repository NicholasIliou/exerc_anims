# Exercise Animation Pipeline - Dependencies

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
mediapipe>=0.10.0

# Optional: YOLO-based equipment detection (run with --use_yolo flag)
# Install with: pip install ultralytics
# ultralytics>=8.0.0
tqdm>=4.65.0
